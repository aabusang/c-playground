#include <stdio.h>


void sort(int *arr, int size);
int main(void)
{
    int *arr1[] = {1, 20, 5, 12, 8};
    sort(arr1);
}

void sort(int *arr, int size)
{
    int i, j, tmp;
    for (i = 0; i < size; i++)
    {
	for (j = i + 1; j < size; j++)
	{
	    
	}
    }
}

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[])
{
    char *arr[] = malloc(sizeof(char *));

    for (int i = 0; i < argc; i++)
    {
	arr[i] = malloc(sizeof(arr[i]));
	strcpy(arr[i], argv[i]);
    }
}

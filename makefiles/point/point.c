#include <stdio.h>
#include "point.h"

point p_value(double x, double y)
{
    point initialized = {a, b};
    return initialized;
}

void print_point(point *self)
{
    print("%f, %f", self->x, self->y)
}
